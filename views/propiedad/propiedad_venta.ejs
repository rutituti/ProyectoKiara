<%- include('../includes/head.ejs') %>
                <br>
                <br>
                <br>
                <br> 
            <div class="row p-3">
              <div class="col-sm-12 col-md-3 col-lg-2"></div>
              <div class="col-sm-12 col-md-7 col-xl-8">
                <div class="input-group mb-3">
                  <input type="text" id="text" for="text" class="form-control" placeholder="Ingresa ubicación o caracteristica">
                  <button class="btn btn-primary" type="button" id="Buscar">Buscar</button>
                </div>
              </div>
              <div class="col-sm-12 col-md-7 col-xl-2"></div>
            </div>
                <div class="w-100">
                    <div id="resultado">
                    <div>                    
                        <% if (propiedades.length > 0) { %>
                            <% for (let c of propiedades) { %>
                                <div row="col-sm-2 col-md-2 col-lg-2">
                                    <%- include('propiedadRentaform.ejs', {propiedades: c}) %>
                                </div>
                            <% } %>
                        <% } else { %>
                            <h3>No hay casas registradas</h3>
                        <% } %>
                    </div>
                </div>
            </div>

<%- include('searchV_ajax.ejs') %>
<%- include('deleteP_ajax.ejs') %>
<%- include('../includes/foot.ejs') %>